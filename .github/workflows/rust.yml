name: Rust

on:
  push:
    branches: ['main', 'dev']
    tags:
      - 'v*.*.*'
  pull_request: {}

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-24.04

    steps:
      - uses: actions/checkout@v5

      - name: Setup Rust
        run: |
          rustup toolchain install stable
          rustup default stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.os }}
          cache-all-crates: 'true'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Build Svelte islands
        run: |
          bun install
          bun run web:islands:build

      - name: Check SvelteKit UI
        working-directory: src/web/ui
        run: |
          bun install
          bun run check

      - name: Build backend
        run: cargo +stable build --workspace

  test:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v5

      - name: Setup Rust
        run: |
          rustup toolchain install stable
          rustup default stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ubuntu-24.04
          cache-all-crates: 'true'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install cargo-llvm-cov
        run: cargo +stable install --locked cargo-llvm-cov

      - name: Build Svelte islands
        run: |
          bun install
          bun run web:islands:build

      - name: Check SvelteKit UI
        working-directory: src/web/ui
        run: |
          bun install
          bun run check

      - name: Run tests
        run: RUST_BACKTRACE=1 cargo +stable test -p api -p repos -p processor -p curator -p web --no-fail-fast
